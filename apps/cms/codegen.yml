overwrite: true
# schema: "https://lokus-api.onrender.com/graphql"
schema: "http://localhost:3001/graphql"
# documents: "src/**/*.graphql"
generates:
    types/generated/types.ts:
        plugins:
            - "typescript"
