overwrite: true
schema: "http://localhost:3001/graphql"
# documents: "src/**/*.graphql"
generates:
    types/generated/types.ts:
        plugins:
            - "typescript"
    # ./graphql.schema.json:
    #     plugins:
    #         - "introspection"
