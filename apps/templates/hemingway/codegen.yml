overwrite: true
schema: "http://localhost:3005/graphql"
# documents: "src/**/*.graphql"
generates:
    types/generated/types.ts:
        plugins:
            - "typescript"
    # ./graphql.schema.json:
    #     plugins:
    #         - "introspection"
